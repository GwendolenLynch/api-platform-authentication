doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'
    orm:
        auto_generate_proxy_classes: true
        naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
        auto_mapping: true
        mappings:
            default:
                type: attribute
                dir: '%kernel.project_dir%/Entity'
                prefix: 'Camelot\Api\Authentication\Tests\Fixtures\Entity'
#            Authentication:
#                is_bundle: false
#                type: attribute
#                dir: '%kernel.project_dir%/Entity'
#                prefix: 'Camelot\Api\Authentication\Entity'
#                alias: Authentication
